import { createHash } from 'node:crypto';
import { readFileSync, writeFileSync } from 'node:fs';
import path from 'node:path';
const root=process.cwd(),backup=path.join(root,'backups/najran_13_targeted_removal_2026-08-25'),load=(file)=>JSON.parse(readFileSync(file,'utf8')),unwrap=(raw)=>Array.isArray(raw)?raw:raw.items,sha=(file)=>createHash('sha256').update(readFileSync(file)).digest('hex'),assert=(ok,message)=>{if(!ok)throw new Error(message)};
const main=unwrap(load(path.join(root,'items.json'))),published=unwrap(load(path.join(root,'client/public/items.json'))),before=unwrap(load(path.join(backup,'items.main.before.json'))),beforePublished=unwrap(load(path.join(backup,'items.published.before.json'))),stats=load(path.join(root,'stats.json')),statsPublic=load(path.join(root,'client/public/stats.json'));const targets=['najran_23056','najran_art_27190','najran_art_35174','najran_art_52092','najran_art_54322','najran_art_62421','najran_art_62426','najran_art_65656','najran_art_65958','najran_art_72236','najran_art_65817','najran_art_67668','najran_art_67290'],source='قسم الأنظمة - جامعة نجران',channel='t.me/c/1876374106',inScope=(item)=>item&&item.source===source&&[item.link_telegram,item.link_drive,item.link_direct].some(link=>String(link??'').includes(channel)),ids=(list)=>new Set(list.map(item=>item.id));const mainIds=ids(main),publicIds=ids(published),beforeIds=ids(before),beforePublicIds=ids(beforePublished),removedFromBefore=[...beforeIds].filter(id=>!mainIds.has(id)).sort(),removedFromBeforePublic=[...beforePublicIds].filter(id=>!publicIds.has(id)).sort();
assert(main.length===11270&&published.length===11270,'العدد النهائي لا يساوي 11,270.');assert(main.filter(inScope).length===34&&published.filter(inScope).length===34,'عدد مواد جامعة نجران لا يساوي 34.');assert(new Set(main.map(item=>item.id)).size===main.length&&new Set(published.map(item=>item.id)).size===published.length,'توجد IDs مكررة.');assert(mainIds.size===publicIds.size&&[...mainIds].every(id=>publicIds.has(id)),'النسختان الرئيسية والمنشورة غير متطابقتين.');assert(targets.every(id=>!mainIds.has(id)&&!publicIds.has(id)),'بقي أحد المعرفات المحذوفة.');assert(JSON.stringify(removedFromBefore)===JSON.stringify([...targets].sort())&&JSON.stringify(removedFromBeforePublic)===JSON.stringify([...targets].sort()),'فُقد سجل خارج قائمة الحذف.');assert(stats.total_items===11270&&statsPublic.total_items===11270&&JSON.stringify(stats)===JSON.stringify(statsPublic),'الإحصاءات غير محدثة أو غير متطابقة.');const result={passed:true,total_items:main.length,najran_count:main.filter(inScope).length,removed_ids_remaining_main:targets.filter(id=>mainIds.has(id)),removed_ids_remaining_published:targets.filter(id=>publicIds.has(id)),main_published_same_ids:true,no_duplicate_ids_main:true,no_duplicate_ids_published:true,no_external_loss:true,hashes:{items_main:sha(path.join(root,'items.json')),items_published:sha(path.join(root,'client/public/items.json')),stats_main:sha(path.join(root,'stats.json')),stats_published:sha(path.join(root,'client/public/stats.json'))}};writeFileSync(path.join(root,'najran_13_removal_post_validation.json'),`${JSON.stringify(result,null,2)}\n`);console.log(JSON.stringify(result,null,2));
